/** HttpOnly cookie set by middleware for the anonymous session being upgraded. */
export const ANON_CLAIM_COOKIE = "zippy_anon_claim"

/** Client-side localStorage hint so StoryClaimer knows when to call claim-stories. */
export const ANON_CLAIM_KEY = "zippy_anon_uid"
